export interface Booking {
    bookingId: number;
    customerId: number;
    carId : Number;
    startDate: string; 
    endDate: string;   
    status: number;    
    createdDate: string; 
    // imageFilePath: string;
}