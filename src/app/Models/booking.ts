export interface Booking {
    bookingId: number;
    customerId: number;
    carId : Number;
    startDate: string; 
    endDate: string;   
    status: string;    
    createdDate: string; 
    // imageFilePath: string;
}