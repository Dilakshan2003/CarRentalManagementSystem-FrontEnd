export interface Booking {
    bookingId: number;
    customerId: number;
    startDate: string; 
    endDate: string;   
    status: number;    
    createdDate: string; 
    // imageFilePath: string;
}